<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <!-- FontAwesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/add-responsable.css">
</head>
<style>
  .table-controls {
   display: flex;
   justify-content: space-between; /* Align controls to the right */
   padding: 10px 70px;
   margin-bottom: 1px;
}

.arrow{
   font-size: 25px;
}
.arrow a{
   font-size: 30px;
   color: black;
}
</style>
<body>
  <!-- Header -->
  <%- include('../partials/header.ejs') %>
  <!-- Header Ends!! -->
  <div class="table-controls">  

    <div class="arrow">
        <a href="/cardcar">
            <i class="fas fa-arrow-left"></i>
        </a>
    </div>
</div>
  <div class="container center-card">
    <section>
      <div class="container">
        <div class="card shadow">
          <div class="card-header border-bottom">
            <h5 class="mb-0">Modifier car</h5>
          </div>
          <div class="card-body">
            <form action="/edite-car/<%= carinfo._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
              <div class="row g-3">
                <div class="col-md-6">
                  <label class="form-label">Name Compagnies</label>
                  <input class="form-control" type="text" name="name_companies" value="<%= carinfo.name_companies %>" placeholder="Entrez Nom Compagnies" required>
                </div>
                <div class="col-md-6">
                  <label class="form-label">Prix</label>
                  <input class="form-control" type="number" name="prix" value="<%= carinfo.prix %>" placeholder="Entrez Prix" required>
                </div>
                <div class="col-md-6">
                  <label class="form-label">Lieu de ramassage</label>
                  <input class="form-control" type="text" name="Lieu_de_ramassage" value="<%= carinfo.Lieu_de_ramassage %>" placeholder="Entrez Lieu de ramassage" required>
                </div>
                <div class="col-md-6">
                  <label class="form-label">Date de ramassage</label>
                  <input class="form-control" type="date" name="Date_de_ramassage" value="<%= carinfo.Date_de_ramassage %>" placeholder="Entrez Date de ramassage" required>
                </div>
                <div class="col-md-6">
                  <label class="form-label">Nom de car</label>
                  <input class="form-control" type="text" name="name_car" value="<%= carinfo.name_car %>" placeholder="Entrez Nom de car" required>
                </div>
                <div class="col-md-6">
                  <label class="form-label">Car Model</label>
                  <input class="form-control" type="text" name="car_model" value="<%= carinfo.car_model %>" placeholder="Entrez Nom de car" required>
                </div>
                <div class="col-md-6">
                  <label class="form-label">Username</label>
                  <input class="form-control" type="text" name="username" value="<%= carinfo.username %>" placeholder="Entrez Nom d'utilisateur">
                </div>
                <div class="col-md-6">
                  <label class="form-label">Temps</label>
                  <input class="form-control" type="time" name="Temps" value="<%= carinfo.Temps %>" placeholder="Entrez Temps" required>
                </div>
                <div class="col-md-6">
                  <label class="form-label">Type</label>
                  <select class="form-control" name="type" required>
                    <option value="diesel" <%= carinfo.type === 'diesel' ? 'selected' : '' %>>Diesel</option>
                    <option value="gasoil" <%= carinfo.type === 'gasoil' ? 'selected' : '' %>>Gasoil</option>
                  </select>
                </div>
                <div class="col-md-6">
                  <label class="form-label">Photo</label>
                  <input class="form-control" type="file" name="photo" >
                </div>
                <div class="col-md-6">
                  <label class="form-label">Nombre Seats</label>
                  <input class="form-control" type="text" name="nombre_seats" value="<%= carinfo.nombre_seats %>" placeholder="Entrez Nombre Seats" required>
                </div>
                <div class="col-md-6">
                  <label class="form-label">Description</label>
                  <input class="form-control" type="text" name="description" value="<%= carinfo.description %>" placeholder="Entrez Description" required>
                </div>
              </div>
              <div class="mt-3">
                <button type="submit" class="btn btn-primary" style="margin: 10px;">Sauvegarder</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </section>
  </div>
  <!-- Footer -->
  <%- include('../partials/footer.ejs') %>
  <!-- Footer End !! -->
</body>
</html>
