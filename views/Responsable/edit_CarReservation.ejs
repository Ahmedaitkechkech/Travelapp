<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit car Reservation</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Poppins:wght@400;500;700&display=swap">
  <link rel="stylesheet" href="/css/add-responsable.css">
  <link rel="stylesheet" href="/css/footer.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">

</head>
<style>
  .table-controls {
   display: flex;
   justify-content: space-between; /* Align controls to the right */
   padding: 10px 70px;
   margin-bottom: 1px;
}

.arrow{
   font-size: 25px;
}
.arrow a{
   font-size: 30px;
   color: black;
}
</style>
<body>
    <!-- Header -->
    <%- include('../partials/header.ejs') %>
    <!-- Header Ends!! -->
    <div class="table-controls">  

      <div class="arrow">
          <a href="/carReservationList">
              <i class="fas fa-arrow-left"></i>
          </a>
      </div>
  </div>
  <div class="container center-card">
    <section>
      <div class="container">
        <div class="card shadow">
          <div class="card-header border-bottom">
            <h5 class="mb-0">Edit Car Reservation</h5>
          </div>
          <div class="card-body">
            <form method="POST" action="/edite_carReservation/<%= CarReservInfo._id %>?_method=PUT">
                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label">name_companies</label>
                        <input class="form-control" type="text" name="name_companies" placeholder="Entrez name_compagnies" value="<%= CarReservInfo.name_companies.name_companies %>" required>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">nombre_jour</label>
                        <input class="form-control" type="number" name="nombre_jour" placeholder="Entrez votre nombre_jour" value="<%= CarReservInfo.nombre_jour %>" required>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Email</label>
                        <input class="form-control" type="email" name="Email" placeholder="Entrez votre Email" value="<%= CarReservInfo.Email %>" required>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Numéro Téléphone</label>
                        <input class="form-control" type="tel" name="tele" placeholder="Numéro Téléphone" value="<%= CarReservInfo.tele %>" required>
                    </div>
                    <div class="col-md-6">
                        <label for="genre">Genre:</label>
                        <select id="genre" name="genre" class="form-control" required>
                            <option value="femme" <%= CarReservInfo.genre === 'femme' ? 'selected' : '' %>>Femme</option>
                            <option value="homme" <%= CarReservInfo.genre === 'homme' ? 'selected' : '' %>>Homme</option>
                        </select>
                    </div>
                
                    <div class="mt-3">
                        <button type="submit" class="btn btn-primary">Sauvegarder</button>
                    </div>
                </div>
            </form>
            
          </div>
        </div>
      </div>
    </section>
  </div>
  <%- include('../partials/footer.ejs') %>
</body>
</html>
