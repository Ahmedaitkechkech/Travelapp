<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Flight Reservation</title>
  <!-- FontAwesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">

  <link rel="stylesheet" href="/css/add-responsable.css">
</head>
<style>
  .table-controls {
   display: flex;
   justify-content: space-between; /* Align controls to the right */
   padding: 10px 70px;
   margin-bottom: 1px;
}

.arrow{
   font-size: 25px;
}
.arrow a{
   font-size: 30px;
   color: black;
}
</style>
<body>
  <!-- Header -->
  <%- include('../partials/header.ejs') %>
  <!-- Header Ends!! -->
       <div class="table-controls">  

        <div class="arrow">
            <a href="/FlightReservationList">
                <i class="fas fa-arrow-left"></i>
            </a>
        </div>
    </div>
  
  <div class="container center-card">
    <section>
      <div class="container">
        <div class="card shadow">
          <div class="card-header border-bottom">
            <h5 class="mb-0">Edit Flight Reservation</h5>
          </div>

          <div class="card-body">
            <form action="/edite_flightReservation/<%= flightReservInfo._id %>?_method=PUT" method="POST">
                <div class="row g-3">
                  <div class="col-md-6">
                    <label class="form-label">Nom des Compagnies</label>
                    <input class="form-control" type="text" value="<%= flightReservInfo.name_compagnies.name_compagnies %>" readonly>
                  </div>
                  <div class="col-md-6">
                    <label class="form-label">Email</label>
                    <input value="<%= flightReservInfo.Email %>" class="form-control" type="email" name="Email" placeholder="Enter Email">
                  </div>
                  <div class="col-md-6">
                    <label class="form-label">Genre</label>
                    <select class="form-control" name="genre">
                      <option value="femme" <%= flightReservInfo.genre === 'femme' ? 'selected' : '' %>>Femme</option>
                      <option value="homme" <%= flightReservInfo.genre === 'homme' ? 'selected' : '' %>>Homme</option>
                    </select>
                  </div>
                  <div class="col-md-6">
                    <label class="form-label">Téléphone</label>
                    <input value="<%= flightReservInfo.tele %>" class="form-control" type="text" name="tele" placeholder="Enter Telephone">
                  </div>
                  <div class="col-md-6">
                    <label class="form-label">Lieu Départ</label>
                    <input value="<%= flightReservInfo.lieu_depart %>" class="form-control" type="text" name="lieu_depart" placeholder="Enter Departure Location" readonly>
                  </div>
                  <div class="col-md-6">
                    <label class="form-label">Heure Départ</label>
                    <input value="<%= flightReservInfo.heure_depart %>" class="form-control" type="text" name="heure_depart" placeholder="Enter Departure Time" readonly >
                  </div>
                  <div class="col-md-6">
                    <label class="form-label">Lieu Arrivée</label>
                    <input value="<%= flightReservInfo.lieu_arrivee %>" class="form-control" type="text" name="lieu_arrivee" placeholder="Enter Arrival Location" readonly>
                  </div>
              

                <div class="mt-3">
                  <button type="submit" class="btn btn-primary">Save Changes</button>
                </div>
            </form>
          </div>
        </div>
      </div>
    </section>
  </div>
  
  <!-- Footer -->
  <%- include('../partials/footer.ejs') %>
  <!-- Footer End !! -->
</body>
</html>
