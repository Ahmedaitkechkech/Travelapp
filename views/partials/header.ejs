<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sticky Header</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <!-- FontAwesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        /* General styling for the header */
        .header-sticky {
            position: sticky;
            top: 0;
            z-index: 1020;
            background-color: #f8f9fa; /* Light background color for navbar-light */
        }

        /* Navbar brand item */
        .navbar-brand-item {
            height: 40px; /* Adjust height as needed */
        }

        /* Light mode and dark mode items */
        .light-mode-item {
            display: block;
        }

        .dark-mode-item {
            display: none;
        }

        /* Language dropdown */
        #languageDropdown {
			color: #4f4f4f;
            font-size: 1.5rem; /* Increase font size */ /* Increase padding */
            height: 20px; /* Adjust height */
            width: 40px; /* Adjust width */
			padding-top: 10px;
        }

        #languageDropdown .dropdown-menu {
            min-width: auto;
            padding: 0.5rem;
        }

        #languageDropdown .dropdown-item {
            display: flex;
            align-items: center;
        }

        #languageDropdown .dropdown-item img {
            width: 20px; /* Adjust as needed */
            height: auto;
        }

        /* Sign-up button */
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }

        /* Ensure the dropdown menus and tooltips are shown correctly */
        .dropdown-menu-end {
            right: 0;
            left: auto;
        }

        /* Responsive adjustments */
        @media (prefers-color-scheme: dark) {
            .light-mode-item {
                display: none;
            }

            .dark-mode-item {
                display: block;
            }

            .navbar-light {
                background-color: white;
            }

            .btn-light {
                
                color: #080000;
            }

           
        }
    </style>
</head>
<body>

    <header>
        <a class="navbar-brand" href="#">
            <img src="/img/logo.svg" alt="logo">
        </a>
        <div class="language-switcher">
            <a href="#" class="logout-link">Logout</a>
            <a href="#" class="language-icon"><i class="bi bi-translate"></i></a>
        </div>
    </header>

    <!-- Your content here -->

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>
</html>
